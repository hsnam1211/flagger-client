import {
  __commonJS
} from "./chunk-LQ2VYIYD.js";

// node_modules/@hoosss-test/utils/dist/index.js
var require_dist = __commonJS({
  "node_modules/@hoosss-test/utils/dist/index.js"(exports, module) {
    var __awaiter = exports && exports.__awaiter || function(thisArg, _arguments, P, generator) {
      function adopt(value) {
        return value instanceof P ? value : new P(function(resolve) {
          resolve(value);
        });
      }
      return new (P || (P = Promise))(function(resolve, reject) {
        function fulfilled(value) {
          try {
            step(generator.next(value));
          } catch (e) {
            reject(e);
          }
        }
        function rejected(value) {
          try {
            step(generator["throw"](value));
          } catch (e) {
            reject(e);
          }
        }
        function step(result) {
          result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
        }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
      });
    };
    function hi(text) {
      return `안녕하세요? ${text}`;
    }
    function setId(id) {
      console.log(id);
    }
    var originalFetch = window.fetch;
    window.fetch = (...args) => __awaiter(void 0, void 0, void 0, function* () {
      const [resource, config] = args;
      const response = yield originalFetch(...args);
      sendLogToServer({
        resource,
        method: (config === null || config === void 0 ? void 0 : config.method) || "GET",
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        status: response.status
      });
      return response;
    });
    function sendLogToServer(log) {
      console.log(log);
    }
    document.addEventListener("click", (event) => {
      const target = event.target;
      const log = {
        element: target.tagName,
        // 클릭한 요소의 태그명
        id: target.id || null,
        // 요소의 ID
        classes: target.className || null,
        // 요소의 클래스
        text: target.textContent || null,
        // 요소의 텍스트 내용
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
        // 클릭 시간
      };
      sendLogToServer(log);
    });
    var Hnormal = class {
      constructor() {
        this.init = () => {
          console.log("HNormal connected!");
        };
      }
    };
    module.exports = { hi, setId, Hnormal };
  }
});
export default require_dist();
//# sourceMappingURL=@hoosss-test_utils.js.map
